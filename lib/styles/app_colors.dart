import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF2196F3);  // Azul primario
  static const Color buttonTextColor = Colors.white;
  static const Color disabledColor = Color(0xFFBDBDBD);  // Gris para botones deshabilitados
  static const Color disabledTextColor = Color(0xFF757575);  // Gris oscuro para texto deshabilitado
}
